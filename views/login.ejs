<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login - Applications Coming Soon</title>

  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>

  <!-- Font Awesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />

  <style>
    .spin-slow {
      animation: spin 6s linear infinite;
    }

    @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
    }
  </style>
</head>
<body class="bg-white min-h-screen flex items-center justify-center px-4">
  <div class="w-full max-w-6xl bg-white shadow-md rounded-3xl overflow-hidden grid grid-cols-1 md:grid-cols-2">

    <!-- Left Panel -->
    <div class="bg-[#000026] text-white p-10 flex flex-col justify-center items-center space-y-6">
      <div class="flex space-x-6 mb-4">
        <i class="fas fa-gear text-6xl text-yellow-400 spin-slow"></i>
        <i class="fas fa-gear text-6xl text-yellow-500 spin-slow -ml-2"></i>
      </div>
      <h2 class="text-3xl font-bold text-center">We’re Still Gearing Up!</h2>
      <p class="text-center text-lg text-gray-200">
        We are still accepting new applications. <br/>
        You will be notified once learning officially begins.
      </p>
      <div class="text-sm text-yellow-300 font-medium">
        <i class="fas fa-envelope mr-2"></i>
        All accepted applicants will be notified via email.
      </div>
    </div>

    <!-- Right Panel -->
    <div class="p-10 flex flex-col justify-center space-y-6">
      <div class="text-center mb-6">
        <h2 class="text-2xl font-bold text-gray-800">Student Portal Login</h2>
        <p class="text-sm text-gray-500">Login will be enabled once applications are processed.</p>
      </div>

      <!-- Form -->
      <form class="space-y-4">

        <!-- Username -->
        <div class="text-left">
          <label class="block text-sm font-medium text-gray-700 mb-1">Student Number</label>
          <div class="relative">
            <i class="fas fa-user absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
            <input
              type="text"
              value="2512345688"
              readonly
              class="w-full pl-10 pr-4 py-3 bg-gray-100 text-gray-500 cursor-not-allowed border border-gray-300 rounded-md"
            />
          </div>
        </div>

        <!-- ID or Email -->
        <div class="text-left">
          <label class="block text-sm font-medium text-gray-700 mb-1">ID or Email</label>
          <div class="relative">
            <i class="fas fa-id-card absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
            <input
              type="text"
              value="studentNumber@ess.com"
              readonly
              class="w-full pl-10 pr-4 py-3 bg-gray-100 text-gray-500 cursor-not-allowed border border-gray-300 rounded-md"
            />
          </div>
        </div>

        <!-- Password -->
        <div class="text-left">
          <label class="block text-sm font-medium text-gray-700 mb-1">Password</label>
          <div class="relative">
            <i class="fas fa-lock absolute left-3 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
            <input
              type="password"
              value="Password123"
              readonly
              class="w-full pl-10 pr-4 py-3 bg-gray-100 text-gray-500 cursor-not-allowed border border-gray-300 rounded-md"
            />
          </div>
        </div>

        <!-- Forgot password and login button -->
        <div class="flex justify-between items-center text-sm mt-2">
          <span class="text-blue-600 cursor-not-allowed opacity-50">Forgot Password?</span>
          <button
            type="button"
            disabled
            class="bg-gray-300 text-gray-500 px-4 py-2 rounded-md cursor-not-allowed"
          >
            Login
          </button>
        </div>
      </form>

      <!-- Divider -->
      <div class="border-t pt-4 text-sm text-gray-400 text-center">
        Access will be granted once onboarding begins.
      </div>

      <!-- Back Button -->
      <div class="pt-4 text-center">
        <a
          href="/"
          class="inline-block bg-black text-white px-6 py-3 rounded-md hover:bg-gray-800 transition-all duration-200"
        >
          <i class="fas fa-arrow-left mr-2"></i> Back to Home
        </a>
      </div>
    </div>

  </div>
</body>
</html>

